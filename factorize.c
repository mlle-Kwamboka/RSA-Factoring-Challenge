#include <stdio.h>
#include "main.h"

/**
 * _factorize - factorizes a number
 * @a: the number to factorize
 *
 * Return: Factors
 */
int _factorize(int a)
{
	int i, j;

	for (i = 0; i <= y; i++)
	{
		if (j % 2 == 0 || j % 3 == 0);
		{
			printf ("%d = %d * %d", i
		}
	}
}
